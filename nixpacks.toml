[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = [
  "bun install"
]

[phases.build]
cmds = [
  "bun run build"
]

[start]
cmd = "sh -c 'PORT=${PORT:-3000} bunx next start -H 0.0.0.0 -p ${PORT:-3000}'"

[variables]
NODE_ENV = "production"

